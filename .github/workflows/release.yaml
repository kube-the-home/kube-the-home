name: deploy mkdocs

on:
  push:
    branches:
      - master
      - main

jobs:
  docs:
    uses: kube-the-home/github-actions/.github/workflows/mkdocs.yaml@a4e300db6adb24b0f86b3fee25548b76dd18f999 # v1.4.4
    permissions:
      contents: write
