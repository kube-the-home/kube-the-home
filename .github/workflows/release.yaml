name: deploy mkdocs

on:
  push:
    branches:
      - master
      - main

jobs:
  docs:
    uses: kube-the-home/github-actions/.github/workflows/mkdocs.yaml@70b35bb255a89f5a9281934be8941da086031276 # v1.4.11
    permissions:
      contents: write
